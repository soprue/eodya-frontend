interface StatusInfoProps {
  previewUrl: any;
  onNext: (data: any) => void;
}

function StatusInfo({ previewUrl, onNext }: StatusInfoProps) {
  return <div>StatusInfo</div>;
}

export default StatusInfo;
